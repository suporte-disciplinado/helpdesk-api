spring:
  datasource:
    # url: jdbc:postgresql://db:5432/helpdeskdb
    # url: jdbc:postgresql://localhost:30009/helpdeskdb
    url: jdbc:postgresql://postgres-service-cluster:5432/helpdeskdb
    username: user
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
server:
  port: 8080